
    <div id="aviso_upload" class="callout callout-info aviso_upload">
        <h4>{{ isset($titulo) ? $titulo : 'Atenção!'}}</h4>
        <p>{{ isset($conteudo) ? $conteudo : ' Para que as fotos sejam exibidas corretamente em todos os dispositivos, as imagens devem ser quadradas, idealmente 500x500.  '}}</p>
    </div>
